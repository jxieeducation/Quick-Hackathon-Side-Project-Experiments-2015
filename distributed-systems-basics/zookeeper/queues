1. desinate a znode to hold the queue (called the queue node) --> create /queue
2. distributed clients call create on a path --> create -e -s /queue/queue-X (note: X is generated by the -s flag)

3. worker clients can now: remove from /queue/queue-X by lowest seq numbers first

